import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  main: {
    flex: 1,
    padding: 20,
    alignItems: "center",
    justifyContent: "center",
  
  },
  mainScroll: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    height: "auto",

    
  },
  mainScrolling: {
    minWidth: "100%",
    padding: 20,
    minHeight: "100%"
  }
});
